// Tailwind CSS configuration
tailwind.config = {
  theme: {
    extend: {
      colors: {
        primary: "#1E3A8A",
        secondary: "#DBEAFE",
      },
    },
  },
};

// Initialize Feather icons when the page loads
feather.replace();
